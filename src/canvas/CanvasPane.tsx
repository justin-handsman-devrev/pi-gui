/**
 * CanvasPane — placeholder component.
 * Will be replaced by the canvas-team agent with a full file-tabbed code editor.
 */
export default function CanvasPane() {
  return (
    <div className="flex h-full items-center justify-center text-[#484f58] text-sm">
      Canvas — no file open
    </div>
  );
}
