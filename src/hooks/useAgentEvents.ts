/**
 * useAgentEvents — placeholder hook.
 * Will be replaced by another agent with actual Tauri event listeners
 * that populate agentStore.
 */
export function useAgentEvents() {
  // no-op — real implementation will listen to Tauri events
}
